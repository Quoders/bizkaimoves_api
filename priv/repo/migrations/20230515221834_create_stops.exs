defmodule BizkaimovesApi.Repo.Migrations.CreateStops do
  use Ecto.Migration

  def change do
    #create table(:stops, primary_key: false) do
      #add :id, :binary_id, primary_key: true
      #add :stop_id, :text
      #add :stop_code, :text
      #add :stop_name, :text

    #  timestamps()
    #end
  end
end
