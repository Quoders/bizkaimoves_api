defmodule BizkaimovesApi.Repo.Migrations.CreateRoutes do
  use Ecto.Migration

  def change do
    #create table(:routes, primary_key: false) do
    #  add :id, :binary_id, primary_key: true
    #  add :route_id, :text
    #  add :route_short_name, :text
    #  add :route_desc, :text
    #  add :route_url, :text
    #  add :route_sort_order, :integer

    #  timestamps()
    #end
  end
end
